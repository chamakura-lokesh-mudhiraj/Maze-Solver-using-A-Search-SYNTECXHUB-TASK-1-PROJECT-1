# Maze-Solver-using-A-Search-SYNTECXHUB-TASK-1-PROJECT-1
A Python-based Maze Solver using the A* Search Algorithm to find the shortest path between a start and goal in a grid maze. The project models the maze as nodes, uses heuristics like Manhattan distance, and visualizes the search process and optimal path in the terminal.
